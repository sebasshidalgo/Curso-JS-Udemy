
// Operador Ternario

const autenticado = true;
const puedePagar = false;


console.log( autenticado ? puedePagar ? 'Si está autenticado y puede pagar' : 
'Está autenticado, pero no puede pagar' : 'No está autenticado');

// el ? reemplaza al "if" y los : reemplaza al "else"