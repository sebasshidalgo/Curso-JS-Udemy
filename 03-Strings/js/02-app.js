
// Métodos de String

const producto = "Monitor 20 pulgadas";
console.log(producto);

// Conocer la longitud del string
console.log(producto.length);

// Conocer la posición de un valor dentro de la cadena
console.log(producto.indexOf("s"));

// Conocer si la cadena incluye dicho valor
console.log(producto.includes("Tablet"));
console.log(producto.includes("pulgadas"));
