
// Métodos String

const producto = 'Monitor 20 Pulgadas';

// Para pasar un string todo a mayusculas
console.log(producto.toUpperCase());

// Para pasar un string todo a minusculas
console.log(producto.toLowerCase());

const email = "CORREO@CORREO.COM";
console.log(email.toLowerCase());

// Para convertir un dato de tipo num a un string
const precio = 300;
console.log(precio); // ---> en la consola si el dato es de tipo numerico figura de color violeta/azul
console.log(precio.toString()); // ---> en la consola si el dato es de tipo string figura de color blanco/negro

