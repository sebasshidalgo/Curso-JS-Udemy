
// Objetos

// En vez de generar 3 variables distintas con diferentes datos:

const nombre = "Monitor 20 Pulgadas";
const precio = 300;
const disponible = true;

console.log(nombre);
console.log(precio);
console.log(disponible);

// Utilizando los objetos podemos agrupar todo en una sola variable:

// Creación de Object literal
const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true,
};

console.log(producto);